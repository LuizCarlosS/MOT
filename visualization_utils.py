from typing import List

from dataset import FrameResults

def create_video(frames: List[FrameResults]):
    for frame in frames:
        pass #create video stream from each frame.get_frame_with_bbox()

def create_video_tracks(frames: List[FrameResults]):
    pass

